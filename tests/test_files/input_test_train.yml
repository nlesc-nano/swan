dataset_file:
  tests/test_files/thousand.csv

property: gammas

use_cuda: False

fingerprint: atompair

model:
  fingerprint_size: 2048
  hidden_cells: 1000

torch_config:
  epochs: 20
  batch_size: 100
  optimizer:
    lr: 0.2
    
optimize_hyperparameters:
  False
  
save_dataset:
  False
