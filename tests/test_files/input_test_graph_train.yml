dataset_file:
  tests/test_files/thousand.csv

properties:
  - gammas

use_cuda: False

featurizer:
  molecular_graph

model:
  name: MPNN

torch_config:
  epochs: 5
  batch_size: 100
  optimizer:
    name: sgd
    lr: 0.002
    momentum: 0.0
